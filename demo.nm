::h1: neomark 语法一览

这份文档本身就是语法演示：每一节先说规则，紧跟的段落就是例子。你可以把渲染结果和这里的原文逐段对照。

::h2: 一、块：自然块与调用块

空行切分自然块。所以这一段和上一段是两个块。

同一个自然块里的换行只是软换行，
渲染成 HTML 之后会被折叠成一个空格。

::notice: 以 :: 开头的行（忽略前导缩进）永远开启调用块，哪怕前面没有空行。
  缩进严格大于头部行的行属于块体；缩进一旦不更大，就是下一个块的首行。

::notice type=warning title="小心！": 分隔冒号之后的内容是块体的第一行。
  续行仍然靠缩进，而且缩进以 0 为基准，所以相对缩进被保留。

::code language=rust: fn main() {
  println!("首行直接写在头部行上");
  }

首行内容会作为「一行」重新参与切分，所以同一行能连着开好几层：

::func1: ::func2: ::func3:
  test

调用块也可以换行嵌套：

::outer: 外层首行
  ::inner:
    内层

标题也是调用块：`::h1` ~ `::h6` 共用一条**正则模式** `^h[1-6]$`，注册的是一个**包装器**——标签名取正则匹配到的那一段，类名加 `nm-` 前缀，正文按行内解析。本文各级标题就是这么来的。

::h2: 二、名字、键、值、链接目标：四处都能用引号

::"带 空格的名字" "键 一"=值一 "键:二"="值 二": 名字、键、值都可以包引号，引号内用 \" 转义。
  名字和键里的空格与冒号都不会被当成分隔符。

链接目标同样支持引号，而且引号还能保护里面的箭头：

[[文档 => "a b.md"]] 与 [[a => "b => c"]]，后者整个 b => c 都是目标。

::h2: 三、行内标记

**加粗**、*斜体*、~~删除线~~、`行内代码`、==高亮==、2^10^ 是上标、H~2~O 是下标。

反斜杠转义：\*不是斜体\*、\"这两个引号是直的\"（不转义的话会变成弯引号）。

::h2: 四、代码跨度与数学：同一套包裹逻辑

一个反引号要用两个包住：`` ` ``；两个就要用三个：``` `` ```。

数学完全对应：$$ $ $$ 的内容就是一个 $。

公式内容一律原样：$\int_0^1 x^2\,dx$、$a_i^2$ —— 所以 ^ 在公式里不是上标。

::h2: 五、实体与智能标点

实体：&amp; &hellip; &copy; &alpha;。

智能标点：等一下... 他说"加引号"，2010--2020，他说---真的，还有 don't。

::h2: 六、行内图标与行内调用

这是**两件事**，语法也分开。

`:name:` 是**行内图标**：只带一个名字，显示什么由渲染器查表决定，认不认识决定它是图标还是原样文字。所以 :rocket: 有图，:nope: 就原样。

`{{ }}` 是**行内调用**：交给展开器，括号内部与块头部是同一个解析器，可以带参数与首行内容。没人认领就是错误——和块调用一样：

{{quote author="张 三": **引用**}}

闭合符是独立记号，所以不会吞掉同一行后面的正文：看 {{quote}} 这一句还在。

::h2: 七、链接

最简形式：[[点我 => https://example.com]]

文本里能写行内标记，因为它就是一个自然块：[[**粗体** 和 `代码` => /x]]

多个箭头以最后一个为准：[[a => b => /c]]

链接不能嵌套：[[外层 [[内层 => c]] => d]] 只产出一个链接。

::h2: 八、换行

行尾反斜杠是硬换行：\
这一行会真的另起一行。

而普通的换行只是软换行，
HTML 会把它折叠成一个空格。

::h2: 九、调用块标准库

内置的展开器在 `stdlib` 模块里：标题、引用、代码各一个文件，各一个 `register`。

引用块带 `origin`：它既是一行看得见的出处，也在 `data-origin` 上
（不是 HTML 的 `cite`——按规范那个得是 URL，而作者写的是书名、讲者这类自由文本）：

::quote origin=马丁·路德·金:
  我有一个梦想！

出处**既进属性也进正文**：`data-origin` 给机器读，`<footer>` 那一行给人读，
右对齐、稍小号由 CSS 管。它是真实文本，能选中、能复制、读屏会念——
不用 CSS 的 `::after` 生成，那样关掉样式表出处就没了。

`origin` 也可以不给，两样都不出现：

::quote:
  没有出处的一句引用，正文照旧。

代码块带 `language`，产出 `language-xxx` 类名——那是 highlight.js / Prism 认的写法，
把它交给页面上的高亮脚本即可。**本库不做高亮，也不引入任何 JS。** 块体原样输出：

::code language=neomark: ::notice: 这行是**文字**，代码块里不解析任何东西。

::notice type=warning:
  上面那个框说明：`::notice` 还没有展开器，所以整块变成一个提示框，原文回显在里面。
  这是当前的进度，不是 bug。离变成真东西只差一行注册：
  `Wrap::tag("div").class("nm-notice")`。
