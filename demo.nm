neomark 功能一览

行内标记

**加粗**、*斜体*、~~删除线~~、`行内代码`、==高亮==、2^10^ 是上标、H~2~O 是下标。

数学与图标

数学用美元符号，内部一律原样：$E = mc^2$、$\int_0^1 x^2 dx$。所以 ^ 在公式里不是上标。

Emoji 短码：:rocket: :warning: :smile: :tada:。查不到的短码原样保留：:nope:。

转义与实体

反斜杠可以转义：\*这不是斜体\*。实体：&amp; &hellip; &copy; &alpha; 都能用。

智能标点

等一下... 他说"加引号"，2010--2020，他说---真的，还有 don't。

换行

行尾反斜杠是硬换行：\
这一行会真的另起一行。而普通的换行
只是软换行，HTML 会把它折叠成一个空格。

链接

最简形式：[[点我 => https://example.com]]

链接文本里能写行内标记：[[**粗体** 和 `代码` => /x]]

多个箭头以最后一个为准：[[a => b => /c]]

链接不能嵌套：[[外层 [[内层 => c]] => d]] 只产出一个链接。

目标里可以有查询串：[[搜索 => https://example.com/?q=neomark&lang=zh]]

行内调用

糖形态与全形是同一件事：:smile: 与 {{smile}} 渲染结果完全一样。

全形可以带参数和首行内容，而括号内部与块头部用的是**同一个解析器**：

{{quote author=张三: **引用**}}（没注册 quote 展开器，所以原样回显，但内容照常渲染）

闭合符是独立记号，所以不会吞掉同一行后面的正文：看 {{rocket}} 这一句还在。

::notice: 插一句：块调用的首行内容写法在这儿照样能用。

调用块：分隔冒号之后是第一行

::notice type=warning: 这一行就是块体的第一行。

续行仍然要用缩进，而且缩进以 0 为基准，所以相对缩进被保留：

::code lang=rust: fn main() {
  println!("首行写在头部行上");
  }

上面那个 ::code 的 raw_body 是 "fn main() {\n  println!(...);\n  }"——两格缩进没有被吃掉。

::notice:
  当然，旧的两行写法照样能用，而且公共缩进去掉。

还没有展开器

::notice type=warning:
  任何调用块目前都会渲染成提示框，并把原文回显在里面。
  这是当前的进度，不是 bug。
